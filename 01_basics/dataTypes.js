"use strict"; // treat all JS code as newer version

// alert(3+3) // we are using node.js not browser

console.log("Preeti")

let nm = "preeti"        // String
let number = 23            // integer
let isLoggedIn = false     // boolean
let ok = null              // null
let sure                   // undefined
                           // symbol => React
console.log(typeof nm);
